import 'package:flutter/material.dart';

class Counsulting extends StatelessWidget {
  const Counsulting({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(

      appBar: AppBar(title: Text("Counsulting"),),
    );
  }
}